package playground;

public class SampleData {
    public double[] features;  // store 14 features as double for decimals
    public double yield;

    public SampleData(double[] features, double yield) {
        this.features = features;
        this.yield = yield;
    }
}
